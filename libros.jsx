function libros({nombres}){
    return(
        <div>
            <h2>Libros</h2>
            {nombres.map((nombre.index)=>(
                <div key={index} className="card">
                    <h2>{libros.nombre} {tipoLibro.nombre}</h2>
                </div>
            ))}
        </div>
    )

}

export default libros